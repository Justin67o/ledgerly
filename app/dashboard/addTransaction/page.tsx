

export default function addTransaction() {
    return (
        <div className="p-6 rounded-2xl" style={{ backgroundColor: "var(--bg-card)", border: "1px solid var(--border)" }}>
            <h1 className="text-xl font-semibold mb-4">Add Transaction</h1>
            <p className="text-sm text-muted-foreground">This is where the form to add a new transaction will go.</p>
        </div>
    );
}